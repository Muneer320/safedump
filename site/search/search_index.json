{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Safedump","text":"<p>Local-first crash diagnostics for Python.</p> <p>Capture complete debugging context when your application crashes: local variables, exception chains, thread state, and environment -- then inspect it safely offline.</p> <pre><code>import safedump\n\nsafedump.install()\n# ... your application runs, crashes ...\n# Crash report saved: ~/.safedump/crash-2026-06-25-123456-TypeError-a1b2c3.safedump.json\n</code></pre> <pre><code>$ safedump view\n</code></pre>"},{"location":"#why-safedump","title":"Why Safedump?","text":"<p>Python's traceback tells you where your code crashed. Safedump tells you why -- what the variables contained, which thread was running, what the environment looked like, and what caused the exception chain.</p> Without Safedump With Safedump Ask the user to reproduce the crash Read the crash report file Add <code>print()</code> and redeploy Variables already captured Manually audit for secrets before sharing Automatic redaction built in Guess which thread from logs Thread state captured at crash time SSH in to check environment Environment metadata in every report"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>pip install safedump[view]\n</code></pre> <pre><code>import safedump\n\nsafedump.install()\n</code></pre>"},{"location":"#cli-at-a-glance","title":"CLI at a Glance","text":"Command Description <code>safedump view</code> View the latest crash report (Rich terminal) <code>safedump view --html</code> Export as a self-contained HTML file <code>safedump list --type KeyError</code> Filter crash reports by type <code>safedump stats</code> Aggregate crash statistics <code>safedump doctor</code> Diagnose configuration issues <code>safedump serve</code> Start a local web browser UI"},{"location":"#design-principles","title":"Design Principles","text":"<ul> <li>Local-first -- Everything works offline. No accounts. No cloud.</li> <li>Privacy-first -- Secrets redacted before storage. Reports never   leave your machine unless you share them.</li> <li>Extensible -- Custom serializers, redaction rules, and   notification hooks without modifying core.</li> </ul>"},{"location":"api/","title":"Python API","text":"<p>The public API is defined by <code>safedump.__all__</code>:</p> <pre><code>__all__ = [\n    \"RedactionRule\",\n    \"__version__\",\n    \"capture_exception\",\n    \"configure\",\n    \"disable\",\n    \"enable\",\n    \"install\",\n    \"load_report\",\n    \"register_serializer\",\n    \"test\",\n    \"uninstall\",\n    \"watch\",\n]\n</code></pre> <p>Everything not in <code>__all__</code> is internal and may change without notice.</p>"},{"location":"api/#configurekwargs","title":"configure(**kwargs)","text":"<p>Configure Safedump globally. Call before <code>install()</code>.</p> <pre><code>safedump.configure(\n    preset=\"production\",\n    output_dir=\"~/.safedump\",\n    privacy_tier=1,\n)\n</code></pre> <p>See Configuration for full parameter documentation.</p>"},{"location":"api/#install","title":"install()","text":"<p>Install Safedump crash hooks globally.</p> <p>Replaces <code>sys.excepthook</code>, <code>threading.excepthook</code>, and <code>sys.unraisablehook</code> with the Safedump crash handler. Safe to call multiple times -- subsequent calls are no-ops.</p> <pre><code>safedump.install()\n</code></pre>"},{"location":"api/#uninstall","title":"uninstall()","text":"<p>Restore the original Python exception hooks that were active before <code>install()</code> was called. Safe to call multiple times.</p>"},{"location":"api/#enable","title":"enable()","text":"<p>Alias for <code>install()</code>.</p>"},{"location":"api/#disable","title":"disable()","text":"<p>Alias for <code>uninstall()</code>.</p>"},{"location":"api/#capture_exceptionexcnone-privacy_tiernone-output_dirnone","title":"capture_exception(exc=None, *, privacy_tier=None, output_dir=None)","text":"<p>Capture an exception and write a crash report. If <code>exc</code> is <code>None</code>, captures the currently handled exception via <code>sys.exc_info()</code>.</p> <pre><code>try:\n    result = dangerous_operation()\nexcept Exception:\n    path = safedump.capture_exception()\n    print(f\"Crash captured: {path}\")\n    raise\n</code></pre> <p>Returns: Path to the crash report file, or <code>None</code> if the write failed.</p> <p>Raises: <code>RuntimeError</code> if no exception is available.</p>"},{"location":"api/#watch-privacy_tiernone-output_dirnone-_watch","title":"watch(*, privacy_tier=None, output_dir=None) -&gt; _Watch","text":"<p>Return a context manager that captures exceptions raised within its block without installing global hooks. Exceptions are re-raised after capture.</p> <pre><code>with safedump.watch():\n    dangerous_code()\n</code></pre>"},{"location":"api/#load_reportpath-dict","title":"load_report(path) -&gt; dict","text":"<p>Load a Safedump crash report as a Python dictionary. Supports both <code>.safedump.json</code> and <code>.safedump.json.gz</code> files.</p> <pre><code>report = safedump.load_report(\"crash.safedump.json\")\nprint(report[\"exception\"][\"type\"])\n</code></pre> <p>Raises: <code>FileNotFoundError</code>, <code>ValueError</code> (invalid format).</p>"},{"location":"api/#test-path-none","title":"test() -&gt; Path | None","text":"<p>Self-test. Deliberately raises and captures a test exception.</p> <pre><code>path = safedump.test()\n</code></pre> <p>Raises: <code>RuntimeError</code> if not installed.</p>"},{"location":"api/#register_serializertype_-handler","title":"register_serializer(type_, handler)","text":"<p>Register a custom serializer for a Python type. The handler receives an instance and must return a JSON-serializable value.</p> <pre><code>import numpy as np\n\nsafedump.register_serializer(np.ndarray, lambda a: a.tolist())\n</code></pre>"},{"location":"api/#redactionrule","title":"RedactionRule","text":"<p>A named tuple defining a custom redaction rule.</p> <pre><code>rule = safedump.RedactionRule(\n    pattern=r\"\\b\\d{16}\\b\",  # 16-digit number\n    replacement=\"[CARD-REDACTED]\",\n    apply_to=\"values\",\n)\n</code></pre> <p>Fields:</p> <ul> <li><code>pattern</code> -- regex pattern to match</li> <li><code>replacement</code> -- replacement text (default <code>[REDACTED]</code>)</li> <li><code>apply_to</code> -- <code>\"values\"</code>, <code>\"names\"</code>, or <code>\"both\"</code></li> </ul>"},{"location":"architecture/","title":"Architecture","text":"<p>Safedump's architecture follows a linear pipeline:</p> <pre><code>Exception raised\n      |\n      v\nCapture (crash_handler)\n  - Walk traceback frames\n  - Capture locals, environment, threads\n  - Compute fingerprint\n      |\n      v\nSanitize (sanitize)\n  - Denylist redaction (variable names)\n  - Regex pattern detection (credentials)\n  - Entropy-based detection (opt-in)\n  - Custom user rules\n      |\n      v\nSerialize (serialize)\n  - Convert to JSON via SafedumpEncoder\n  - Handle special types (set, deque, etc.)\n  - Custom serializers via register_serializer()\n      |\n      v\nStore (save)\n  - Generate filename\n  - Check for dedup (same fingerprint?)\n  - Atomic write (tempfile + os.replace)\n  - Optional gzip compression\n      |\n      v\nNotify (on_crash hook)\n  - User-defined callback with report path\n</code></pre>"},{"location":"architecture/#module-layout","title":"Module Layout","text":"Module Responsibility <code>_capture.py</code> Crash handler, install/uninstall, capture_exception <code>_frame_walker.py</code> Traceback walking, frame/environment/thread capture <code>_sanitize.py</code> Secret detection and redaction <code>_serialize.py</code> JSON encoding with custom type support <code>_storage.py</code> File persistence, dedup, compression <code>_loader.py</code> Report loading, schema migration, listing, stats <code>_html_render.py</code> Self-contained HTML report generation <code>_server.py</code> Local web server (stdlib http.server) <code>_render.py</code> Rich terminal output <code>_cli.py</code> Command-line interface (argparse) <code>_config.py</code> Configuration storage and presets <code>_types.py</code> Data model classes and version constants <code>watch.py</code> Context manager for scoped monitoring <code>logging_handler.py</code> Python logging integration"},{"location":"architecture/#crash-pipeline","title":"Crash Pipeline","text":"<p>Each stage of the pipeline is wrapped in exception handlers.</p> <ul> <li>If capture fails, the original traceback is always preserved.</li> <li>If sanitize fails, the report is still saved (unredacted).</li> <li>If save fails, a fallback to the system temp directory is   attempted.</li> <li>If all fails, Python's default exception handling applies.</li> </ul>"},{"location":"architecture/#schema-migration","title":"Schema Migration","text":"<p>Reports contain a <code>schema_version</code> field. When <code>load_report()</code> reads a report, it checks the version and applies any pending migrations sequentially. This ensures backward compatibility as the format evolves.</p>"},{"location":"architecture/#fingerprint-generation","title":"Fingerprint Generation","text":"<p>Fingerprints use SHA256 of <code>exception_type + message[:200] + crash_file + crash_line</code>. This is stable across runs for the same crash at the same location.</p>"},{"location":"changelog/","title":"Changelog","text":"<p>This page lists notable changes to Safedump.</p> <p>For the full changelog, see CHANGELOG.md on GitHub.</p>"},{"location":"changelog/#v130-smart-detection-2026-07-30","title":"v1.3.0 \u2014 Smart Detection (2026-07-30)","text":"<ul> <li>Entropy-based secret detection (opt-in)</li> <li>Crash report deduplication by fingerprint</li> <li>Optional gzip report compression</li> <li><code>on_crash</code> notification hook</li> <li>pytest integration plugin</li> <li>Click/Typer integration (<code>@wrap_click()</code>)</li> <li>181 tests</li> </ul>"},{"location":"changelog/#v120-foundation-shareability-2026-07-30","title":"v1.2.0 \u2014 Foundation &amp; Shareability (2026-07-30)","text":"<ul> <li>Schema versioning and migration framework</li> <li>Crash fingerprint generation</li> <li>HTML crash report export (<code>--html</code>)</li> <li>CLI report filtering (<code>--type</code>, <code>--since</code>)</li> <li><code>safedump doctor</code>, <code>safedump stats</code></li> <li><code>safedump serve</code> local web server</li> <li>Module refactoring (<code>_capture.py</code> split)</li> <li>172 tests</li> </ul>"},{"location":"changelog/#v110-community-integration-2026-07-09","title":"v1.1.0 \u2014 Community &amp; Integration (2026-07-09)","text":"<ul> <li>Framework integration guides (Flask, FastAPI, Django)</li> <li><code>safedump view --json</code> flag</li> <li>Capture-layer edge case tests</li> <li>Improved error handling for missing Rich</li> <li>Dynamic version via importlib.metadata</li> <li>Windows compatibility fixes</li> </ul>"},{"location":"changelog/#v100-stable-api-2026-06-25","title":"v1.0.0 \u2014 Stable API (2026-06-25)","text":"<ul> <li>Stable public API (12 functions)</li> <li>Full crash capture pipeline</li> <li>Secret redaction (denylist + regex)</li> <li>Three exception hooks (sys, threading, unraisable)</li> <li>CLI subcommands (view, list, clean, test)</li> <li>Privacy tiers (0-4)</li> <li>Rich-powered terminal viewer</li> <li>Config presets</li> <li>67 tests across 5 Python versions</li> </ul>"},{"location":"cli/","title":"CLI Reference","text":""},{"location":"cli/#safedump-view","title":"safedump view","text":"<p>View a crash report in the terminal or export as HTML.</p> <pre><code>safedump view [file]                 # View latest or specific report\nsafedump view --json                 # Output raw JSON (pipe to jq)\nsafedump view --html [output.html]   # Export as self-contained HTML\n</code></pre> <p>If no file is specified, the most recent report is used.</p>"},{"location":"cli/#safedump-list","title":"safedump list","text":"<p>List recent crash reports with optional filtering.</p> <pre><code>safedump list                            # Last 20 reports\nsafedump list --count 50                 # Last 50 reports\nsafedump list --type KeyError            # Filter by exception type\nsafedump list --since 7d                 # Reports from last 7 days\nsafedump list --since 2026-07-01         # Reports since date\nsafedump list --search \"timeout\"         # Search in type/message\n</code></pre> <p>Time filters accept ISO dates (<code>2026-07-01</code>) and human-readable durations (<code>7d</code>, <code>24h</code>, <code>30m</code>).</p>"},{"location":"cli/#safedump-stats","title":"safedump stats","text":"<p>Show aggregate crash statistics.</p> <pre><code>safedump stats\n</code></pre> <p>Output includes total crash count, breakdown by exception type, daily distribution, and top crash sites.</p>"},{"location":"cli/#safedump-doctor","title":"safedump doctor","text":"<p>Diagnose common configuration issues.</p> <pre><code>safedump doctor                   # Quick check\nsafedump doctor --verbose         # Detailed diagnostics\n</code></pre> <p>Checks performed:</p> <ul> <li>Python version compatibility</li> <li>Output directory writability</li> <li>Exception hook installation status</li> <li>Crash report integrity</li> <li>Rich terminal viewer availability</li> </ul>"},{"location":"cli/#safedump-serve","title":"safedump serve","text":"<p>Start a local web server for browsing crash reports.</p> <pre><code>safedump serve                        # http://127.0.0.1:4567\nsafedump serve --port 8080            # Custom port\nsafedump serve --host 0.0.0.0         # Network accessible\n</code></pre> <p>The server is intentionally minimal (stdlib <code>http.server</code>). No authentication, no sessions, no JavaScript framework.</p>"},{"location":"cli/#safedump-clean","title":"safedump clean","text":"<p>Delete old crash reports.</p> <pre><code>safedump clean --older-than 30   # Delete reports older than 30 days\n</code></pre>"},{"location":"cli/#safedump-test","title":"safedump test","text":"<p>Verify Safedump is working.</p> <pre><code>safedump test\n</code></pre> <p>Deliberately captures a test exception and writes a report.</p>"},{"location":"cli/#safedump-version","title":"safedump --version","text":"<p>Print the Safedump version.</p> <pre><code>safedump --version\n</code></pre>"},{"location":"contributing/","title":"Contributing","text":"<p>Thank you for your interest in contributing to Safedump!</p>"},{"location":"contributing/#development-setup","title":"Development Setup","text":"<pre><code>git clone https://github.com/Muneer320/safedump.git\ncd safedump\npip install -e \".[dev]\"\npre-commit install\n</code></pre>"},{"location":"contributing/#running-tests","title":"Running Tests","text":"<pre><code>pytest                           # All tests\npytest -q                        # Quiet mode\npytest tests/test_capture.py     # Specific module\npytest -x                        # Stop on first failure\n</code></pre>"},{"location":"contributing/#code-style","title":"Code Style","text":"<p>Safedump uses ruff for both linting and formatting.</p> <pre><code>ruff check .      # Lint\nruff format .     # Format\n</code></pre>"},{"location":"contributing/#type-checking","title":"Type Checking","text":"<pre><code>mypy src/safedump/ --strict\n</code></pre> <p>All code must pass strict mode type checking.</p>"},{"location":"contributing/#pull-request-process","title":"Pull Request Process","text":"<ol> <li>Create a feature branch from <code>main</code>.</li> <li>Make your changes with tests.</li> <li>Run <code>pytest</code> and <code>mypy</code> locally.</li> <li>Create a pull request with a clear description.</li> <li>Ensure CI passes.</li> </ol>"},{"location":"contributing/#architecture","title":"Architecture","text":"<p>See Architecture for an overview of the module layout and crash pipeline.</p>"},{"location":"contributing/#adding-a-new-redaction-pattern","title":"Adding a New Redaction Pattern","text":"<p>Edit <code>src/safedump/_types.py</code> in the <code>DENYLIST_SUBSTRING_MATCH</code> set or the <code>secret_patterns</code> property. Add corresponding tests in <code>tests/test_sanitize.py</code>.</p>"},{"location":"contributing/#writing-integrations","title":"Writing Integrations","text":"<p>Integrations live in <code>src/safedump/integrations/</code> as standalone modules. Each integration should:</p> <ul> <li>Import only what it needs from Safedump.</li> <li>Never modify Safedump's global state.</li> <li>Be importable without side effects.</li> </ul>"},{"location":"deprecation_policy/","title":"Deprecation Policy","text":"<p>This document defines Safedump's commitment to backward compatibility and the process for deprecating and removing features.</p>"},{"location":"deprecation_policy/#semantic-versioning","title":"Semantic Versioning","text":"<p>Safedump follows Semantic Versioning 2.0.0.</p> <ul> <li>Patch releases (1.2.x) \u2014 bug fixes only, no breaking changes.</li> <li>Minor releases (1.x.0) \u2014 new features, deprecations, no breaking changes.</li> <li>Major releases (2.0.0) \u2014 breaking changes allowed.</li> </ul>"},{"location":"deprecation_policy/#what-is-public-api","title":"What is Public API?","text":"<p>The public API is defined by <code>safedump.__all__</code> in <code>__init__.py</code>:</p> <ul> <li><code>RedactionRule</code></li> <li><code>capture_exception</code></li> <li><code>configure</code></li> <li><code>disable</code></li> <li><code>enable</code></li> <li><code>install</code></li> <li><code>load_report</code></li> <li><code>register_serializer</code></li> <li><code>test</code></li> <li><code>uninstall</code></li> <li><code>watch</code></li> </ul> <p>Everything else (modules prefixed with <code>_</code>, internal functions, test utilities) is internal and may change without notice.</p>"},{"location":"deprecation_policy/#cli-commands","title":"CLI Commands","text":"<p>CLI subcommands follow the same versioning as the Python API. Removing or renaming a command requires a major version bump.</p>"},{"location":"deprecation_policy/#deprecation-timeline","title":"Deprecation Timeline","text":"<ol> <li>Deprecation announced \u2014 Function/documentation marked with    <code>deprecated</code> and added to release notes. A <code>DeprecationWarning</code>    is issued at runtime where feasible.</li> <li>Removal allowed \u2014 After one minor version or 6 months,    the feature may be removed in the next major version.</li> </ol> Phase Timeline Status Deprecation warning Minor release N Users see warnings Removal Major release N+1 Feature removed"},{"location":"deprecation_policy/#schema-evolution","title":"Schema Evolution","text":"<p>The crash report schema uses a version number (<code>schema_version</code> field).</p> <ul> <li>New optional fields can be added in any release.</li> <li>Removing or renaming fields requires a major version bump.</li> <li>Old reports are automatically migrated forward (never backward).</li> </ul>"},{"location":"deprecation_policy/#exceptions","title":"Exceptions","text":"<p>Deprecation policy may be bypassed for:</p> <ul> <li>Security fixes that cannot be applied otherwise.</li> <li>Bugs that make a feature permanently non-functional.</li> <li>Removing features that have never worked (documented as experimental).</li> </ul> <p>These exceptions will be documented in release notes with clear justification.</p>"},{"location":"deprecation_policy/#deprecation-in-practice","title":"Deprecation in Practice","text":"<p>When a function is deprecated:</p> <ol> <li>The docstring gains a <code>.. deprecated::</code> directive.</li> <li>A <code>DeprecationWarning</code> is emitted once on first call.</li> <li>The CHANGELOG notes the deprecation.</li> <li>After the deprecation period, the function is removed in a major release.</li> </ol>"},{"location":"faq/","title":"Frequently Asked Questions","text":"<p>Does Safedump send data anywhere?</p> <p>No. Safedump is completely offline. It never makes network connections. Crash reports are stored locally on your filesystem.</p> <p>Is it safe to share crash reports?</p> <p>Yes (after review). By default, Safedump redacts variable names like <code>password</code>, <code>token</code>, and <code>secret</code>, and detects credential patterns. The report includes a redaction audit trail. Still, always review before sharing publicly.</p> <p>What's the performance overhead?</p> <p>Zero during normal execution. Safedump only runs when an unhandled exception occurs. Crash capture takes approximately 4ms for a typical 20-frame traceback.</p> <p>What Python versions are supported?</p> <p>3.9 through 3.13. Tested on all versions in CI on both Linux and Windows.</p> <p>Can I use this in production?</p> <p>Yes. Use <code>configure(preset=\"production\")</code> (privacy tier 1, no env capture, no argv). Safedump is designed to fail gracefully -- if the handler itself crashes, the original traceback is always preserved.</p> <p>How do I capture a crash without installing global hooks?</p> <p>Use <code>safedump.watch()</code> for scoped monitoring, or call <code>safedump.capture_exception()</code> inside <code>except</code> blocks.</p> <p>Can I customize what gets redacted?</p> <p>Yes. Use <code>RedactionRule</code> for custom regex patterns, and configure the privacy tier to control capture detail.</p> <p>Does Safedump support Windows?</p> <p>Yes. Windows 11 is tested in CI. Crash reports fall back to the system temp directory (via <code>tempfile.gettempdir()</code>) instead of hardcoded <code>/tmp</code>.</p> <p>How do I view crash reports from another machine?</p> <p>Copy the <code>.safedump.json</code> file to your machine and run <code>safedump view &lt;path&gt;</code> or <code>safedump view --html &lt;path&gt;</code> to generate a self-contained HTML file.</p> <p>What happens if Safedump itself crashes during capture?</p> <p>The original Python traceback is always preserved. A Safedump internal error message is printed to stderr, followed by the original traceback. Your application's crash information is never lost.</p> <p>How do compressed reports work?</p> <p>When <code>compress=True</code> is configured, reports are saved as <code>.safedump.json.gz</code> files. They are transparently decompressed when read via <code>load_report()</code>. All CLI commands handle both formats.</p> <p>Does Safedump have a plugin system?</p> <p>The <code>register_serializer()</code> function allows custom type serialization. A full plugin system via <code>importlib.metadata</code> entry points is planned for a future release.</p>"},{"location":"html-export/","title":"HTML Export","text":"<p>Safedump can generate self-contained HTML crash reports that work in any browser without an internet connection.</p>"},{"location":"html-export/#basic-usage","title":"Basic Usage","text":"<pre><code>safedump view --html report.html\n</code></pre> <p>This produces a single <code>.html</code> file containing the full crash report with dark theme, collapsible stack frames, and type badges.</p>"},{"location":"html-export/#specify-output-path","title":"Specify Output Path","text":"<pre><code>safedump view --html ~/Desktop/crash-report.html\n</code></pre>"},{"location":"html-export/#html-features","title":"HTML Features","text":"<ul> <li>Self-contained -- Zero external dependencies. No CDN, no Google   Fonts, no external CSS or JS. Works offline forever.</li> <li>Dark theme -- Designed for developer workflows.</li> <li>Collapsible frames -- Stack frames expand/collapse individually.</li> <li>Crash site highlighting -- The crash frame is expanded by   default and marked in red.</li> <li>Variable tables -- Local variables shown with type badges and   values.</li> <li>Copy JSON button -- One-click copy of the raw report.</li> <li>Print-friendly CSS -- Page prints cleanly with white background.</li> <li>System font stack -- No font downloads.</li> </ul>"},{"location":"html-export/#architecture","title":"Architecture","text":"<p>The HTML renderer is a pure function: <code>dict -&gt; str</code>. It takes a loaded crash report and returns an HTML string. The server and CLI both reuse the same renderer -- there is no duplicated formatting logic.</p>"},{"location":"integrations/","title":"Integrations","text":"<p>Safedump provides optional integrations for developer tools. Each integration is a standalone module in <code>safedump.integrations.*</code> -- import only what you need.</p>"},{"location":"integrations/#pytest","title":"pytest","text":"<p>Automatically capture crash reports when tests fail.</p> <pre><code># conftest.py\npytest_plugins = [\"safedump.integrations.pytest_plugin\"]\n</code></pre> <p>Every test failure now produces a structured crash report alongside the normal pytest output.</p>"},{"location":"integrations/#click-typer","title":"Click / Typer","text":"<p>Wrap CLI commands to capture crashes on unhandled exceptions.</p> <pre><code>import click\nfrom safedump.integrations.click_plugin import wrap_click\n\n\n@click.command()\n@wrap_click()\ndef my_command(): ...\n</code></pre> <p>The exception is captured and then re-raised, so Click's normal error handling still works.</p>"},{"location":"integrations/#with-typer","title":"With Typer","text":"<pre><code>import typer\nfrom safedump.integrations.click_plugin import wrap_click\n\napp = typer.Typer()\n\n\n@app.command()\n@wrap_click()\ndef my_command(): ...\n</code></pre>"},{"location":"migration/","title":"Migration Guide","text":""},{"location":"migration/#upgrading-to-v20","title":"Upgrading to v2.0","text":"<p>No breaking changes. v2.0 is a stabilization release. All v1.x public APIs remain functional.</p>"},{"location":"migration/#deprecations","title":"Deprecations","text":"<p>None.</p>"},{"location":"migration/#upgrading-to-v13","title":"Upgrading to v1.3","text":"<p>No breaking changes.</p>"},{"location":"migration/#what-changed","title":"What changed","text":"<ul> <li>New optional <code>enable_entropy_detection</code> and <code>entropy_threshold</code>   config parameters.</li> <li>New optional <code>compress</code> config parameter.</li> <li>New optional <code>on_crash</code> config parameter.</li> <li>New integration modules (<code>safedump.integrations.*</code>).</li> <li>Reports may now be saved as <code>.json.gz</code> when <code>compress=True</code>.</li> </ul>"},{"location":"migration/#migration","title":"Migration","text":"<p>All existing v1.x reports remain readable. No action required.</p>"},{"location":"migration/#upgrading-to-v12","title":"Upgrading to v1.2","text":"<p>No breaking changes.</p>"},{"location":"migration/#what-changed_1","title":"What changed","text":"<ul> <li>Reports now have a <code>schema_version</code> field (v1).</li> <li>Reports now have <code>fingerprint</code>, <code>occurrence_count</code>, <code>first_seen</code>,   <code>last_seen</code> fields.</li> <li><code>metadata</code> field type widened from <code>dict[str, str]</code> to <code>dict[str, Any]</code>.</li> <li>New CLI commands: <code>safedump doctor</code>, <code>safedump stats</code>, <code>safedump serve</code>.</li> <li>New CLI flags: <code>safedump view --html</code>, <code>safedump list --type/--since</code>.</li> </ul>"},{"location":"migration/#migration_1","title":"Migration","text":"<p>Old reports (no <code>schema_version</code>) are automatically migrated on read. No action required.</p>"},{"location":"migration/#upgrading-to-v11","title":"Upgrading to v1.1","text":"<p>No breaking changes.</p>"},{"location":"migration/#what-changed_2","title":"What changed","text":"<ul> <li>Framework integration guides (Flask, FastAPI, Django).</li> <li><code>safedump view --json</code> flag.</li> <li>Improved error handling for missing Rich.</li> <li>Dynamic version via <code>importlib.metadata</code>.</li> </ul>"},{"location":"migration/#upgrading-from-v0x","title":"Upgrading from v0.x","text":"<p>Reports from v0.x (pre-release) may not be forward-compatible with v1.x. It is recommended to regenerate reports rather than relying on migration of pre-release data.</p>"},{"location":"privacy/","title":"Privacy Guide","text":"<p>Safedump is designed to capture debugging context without compromising privacy. This guide explains what's captured at each tier and how to configure Safedump for your environment.</p>"},{"location":"privacy/#privacy-tiers","title":"Privacy Tiers","text":"Tier Name Captures Safe to Share? 0 Minimal Stack trace + exception info only \u2705 Yes 1 Standard (default) Tier 0 + local variables with mandatory redaction \u2705 After visual review 2 Verbose Tier 1 + instance attributes, function arguments \u26a0\ufe0f Review before sharing 3 Full Tier 2 + globals (redacted), env var names \u26a0\ufe0f Review carefully 4 Debug Everything including env var values \u274c Never share without manual audit"},{"location":"privacy/#what-gets-redacted-tier-1","title":"What Gets Redacted (Tier 1+)","text":""},{"location":"privacy/#variable-name-denylist","title":"Variable Name Denylist","text":"<p>Variables whose names contain these patterns are redacted: <code>password</code>, <code>secret</code>, <code>token</code>, <code>key</code>, <code>api_key</code>, <code>credential</code>, <code>auth</code>, <code>private_key</code>, <code>access_token</code>, <code>session_key</code>, <code>database_url</code>, and more.</p> <p>Matching uses tiered logic: \u22643-char patterns match exactly, 4-char patterns match word boundaries, \u22655-char patterns match substrings. This prevents false positives like <code>keyboard</code> matching <code>key</code>.</p>"},{"location":"privacy/#credential-pattern-detection","title":"Credential Pattern Detection","text":"<p>String values matching known credential formats are redacted: - AWS Access Keys (<code>AKIA...</code>) - GitHub Tokens (<code>ghp_...</code>) - Stripe Keys (<code>sk_live_...</code>) - JWT Tokens (<code>eyJ...</code>) - Generic API keys</p>"},{"location":"privacy/#custom-redaction-rules","title":"Custom Redaction Rules","text":"<p>Add domain-specific rules: <pre><code>safedump.configure(\n    redaction_rules=[\n        safedump.RedactionRule(r\"PROPRIETARY-\\d+\", \"[REDACTED]\"),\n        safedump.RedactionRule(r\"\\b\\d{3}-\\d{2}-\\d{4}\\b\", \"[SSN REDACTED]\", \"values\"),\n    ]\n)\n</code></pre></p>"},{"location":"privacy/#what-is-never-captured-by-default","title":"What Is NEVER Captured (by default)","text":"<ul> <li>Environment variable values (names only, at Tier 3+)</li> <li>Command-line arguments (opt-in via <code>include_argv=True</code>)</li> <li>File contents</li> <li>Network traffic</li> <li>Keystrokes</li> </ul>"},{"location":"privacy/#before_capture-hook","title":"<code>before_capture</code> Hook","text":"<p>For maximum control, use the <code>before_capture</code> hook to scrub data before Safedump processes it:</p> <pre><code>def my_scrubber(report):\n    # Remove sensitive frames or values\n    return report\n\n\nsafedump.configure(before_capture=my_scrubber)\n</code></pre>"},{"location":"privacy/#report-security","title":"Report Security","text":"<ul> <li>Files saved with <code>0600</code> permissions (owner read/write only)</li> <li>Directory saved with <code>0700</code> permissions</li> <li>No network access \u2014 reports never leave your machine</li> <li>Redaction audit trail in every report records what was redacted and why</li> </ul>"},{"location":"privacy/#sharing-guidance","title":"Sharing Guidance","text":"<ol> <li>Always run <code>safedump view</code> first \u2014 visually inspect the report</li> <li>Check the redactions section \u2014 see what was automatically scrubbed</li> <li>Use Tier 0 for public sharing \u2014 stack traces contain no variable data</li> <li>For bug reports \u2014 Tier 1 reports are generally safe after visual review</li> <li>Never share Tier 4 reports \u2014 they may contain environment variable values with credentials</li> </ol>"},{"location":"report-format/","title":"Crash Report Format","text":"<p>Crash reports are JSON files with the extension <code>.safedump.json</code> (or <code>.safedump.json.gz</code> when compression is enabled).</p>"},{"location":"report-format/#file-naming","title":"File Naming","text":"<pre><code>{timestamp}-{exception_type}-{hash}.safedump.json\n</code></pre> <p>Example: <code>2026-07-30-12-00-00-ValueError-a1b2c3d4.safedump.json</code></p> <p>The timestamp ensures chronological sorting. The hash provides deduplication without revealing crash details.</p>"},{"location":"report-format/#schema-version","title":"Schema Version","text":"<p>Every report includes a <code>schema_version</code> field. The current version is v1. Old reports are automatically migrated forward when loaded via <code>load_report()</code>.</p>"},{"location":"report-format/#top-level-fields","title":"Top-Level Fields","text":"Field Type Description <code>schema_version</code> int Report schema version (currently 1) <code>safedump_version</code> str Version that captured this report <code>fingerprint</code> str 12-char SHA256 crash identifier <code>timestamp</code> str ISO-8601 capture time <code>python_version</code> str Python version string <code>platform</code> str OS platform (sys.platform) <code>exception</code> object Exception details <code>frames</code> array Stack frames (caller first) <code>environment</code> object Runtime environment snapshot <code>threads</code> array Thread state at capture time <code>redactions</code> array Redaction audit trail"},{"location":"report-format/#exception-object","title":"Exception Object","text":"Field Type Description <code>type</code> str Exception class name (e.g. <code>ValueError</code>) <code>message</code> str Exception message <code>module</code> str Module where exception is defined <code>is_explicitly_chained</code> bool Whether <code>__cause__</code> was set <code>sub_exceptions</code> array Chained/grouped exceptions"},{"location":"report-format/#frame-object","title":"Frame Object","text":"Field Type Description <code>index</code> int Frame number (0 = crash site) <code>file</code> str Source file path <code>line</code> int Line number <code>function</code> str Function name <code>code_context</code> array Source lines around the crash site <code>locals</code> object Local variables (name -&gt; VariableSnapshot) <code>is_crash_site</code> bool True for the crash frame"},{"location":"report-format/#variablesnapshot","title":"VariableSnapshot","text":"Field Type Description <code>type</code> str Type name (e.g. <code>int</code>, <code>str</code>) <code>value</code> str String representation <code>is_truncated</code> bool True if value was truncated <code>original_length</code> int Original length before truncation"},{"location":"report-format/#environment-object","title":"Environment Object","text":"Field Type Description <code>os_name</code> str <code>os.name</code> <code>os_version</code> str <code>sys.platform</code> <code>python_impl</code> str Python implementation <code>python_path</code> array <code>sys.path</code> <code>cwd</code> str Current working directory <code>env_var_names</code> array Environment variable names (opt-in) <code>argv</code> array Command-line arguments (opt-in)"},{"location":"report-format/#fingerprint","title":"Fingerprint","text":"<p>The <code>fingerprint</code> field is a 12-character hex string computed from:</p> <pre><code>SHA256(exception_type + exception_message[:200] + crash_file + crash_line)[:12]\n</code></pre> <p>This is stable across runs for the same crash in the same location. Different crashes produce different fingerprints.</p>"},{"location":"security/","title":"Security &amp; Privacy","text":"<p>Safedump is designed for privacy-first crash diagnostics. It never sends data over the network and provides multiple layers of secret redaction.</p>"},{"location":"security/#network-isolation","title":"Network Isolation","text":"<p>Safedump makes zero network calls. There is no telemetry, no analytics, no phone-home mechanism. Crash reports are stored exclusively on the local filesystem.</p> <p>The only exception is the optional local web server (<code>safedump serve</code>), which binds to <code>127.0.0.1</code> by default and never reaches the network.</p>"},{"location":"security/#secret-redaction","title":"Secret Redaction","text":"<p>Safedump applies three layers of redaction before saving a report:</p>"},{"location":"security/#1-variable-name-denylist","title":"1. Variable Name Denylist","text":"<p>Variable names matching known secret patterns (e.g., <code>password</code>, <code>token</code>, <code>api_key</code>, <code>secret</code>) are redacted by name. The denylist contains 70+ common patterns.</p>"},{"location":"security/#2-regex-credential-detection","title":"2. Regex Credential Detection","text":"<p>Values matching credential patterns are redacted:</p> <ul> <li>AWS Access Keys (<code>AKIA...</code>)</li> <li>GitHub Personal Access Tokens (<code>ghp_...</code>)</li> <li>GitHub Fine-Grained Tokens (<code>github_pat_...</code>)</li> <li>Slack Bot Tokens (<code>xoxb-...</code>)</li> <li>Slack Webhook URLs</li> <li>JWT Tokens (Base64-encoded JSON Web Tokens)</li> <li>RSA Private Keys</li> <li>SSH Private Keys</li> <li>Generic credential patterns</li> </ul>"},{"location":"security/#3-entropy-detection-opt-in","title":"3. Entropy Detection (opt-in)","text":"<p>When enabled, high-entropy strings (random-looking values like API keys that don't match known patterns) are redacted based on Shannon entropy. This detects novel secrets that the denylist and regex patterns miss.</p>"},{"location":"security/#file-permissions","title":"File Permissions","text":"<ul> <li>Report files: <code>0o600</code> (owner read/write only)</li> <li>Output directory: <code>0o700</code> (owner access only)</li> </ul>"},{"location":"security/#redaction-audit-trail","title":"Redaction Audit Trail","text":"<p>Every redaction is recorded in the report's <code>redactions</code> array:</p> <pre><code>{\n    \"redactions\": [\n        {\n            \"location\": \"frames[0].locals.password\",\n            \"reason\": \"matched pattern: ghp_[0-9a-zA-Z]{36}\",\n            \"rule\": \"secret_pattern\",\n            \"timestamp\": \"2026-07-30T12:00:00\"\n        }\n    ]\n}\n</code></pre> <p>This makes it clear what was redacted and why.</p>"},{"location":"security/#privacy-tiers","title":"Privacy Tiers","text":"Tier Description Captures 0 Minimal Exception type, message, crash site 1 Default + local variable values (short) 2 Verbose + longer values, more collection items 3 Debug + environment details 4 Full + env variable names, argv"},{"location":"security/#best-practices","title":"Best Practices","text":"<ol> <li>Always review crash reports before sharing publicly.</li> <li>Use <code>configure(preset=\"production\")</code> in production environments.</li> <li>Enable entropy detection for defense-in-depth:    <code>configure(enable_entropy_detection=True)</code></li> <li>Consider enabling compression for storage efficiency:    <code>configure(compress=True)</code></li> </ol>"},{"location":"web-server/","title":"Local Web Server","text":"<p>Safedump includes a minimal local web server for browsing crash reports in the browser.</p>"},{"location":"web-server/#usage","title":"Usage","text":"<pre><code>safedump serve\n</code></pre> <p>Opens <code>http://127.0.0.1:4567</code> in your default browser.</p>"},{"location":"web-server/#options","title":"Options","text":"<pre><code>safedump serve --port 8080          # Custom port\nsafedump serve --host 0.0.0.0       # Bind to all interfaces\n</code></pre>"},{"location":"web-server/#what-it-provides","title":"What it provides","text":"<ul> <li>Report list -- Shows all crash reports sorted by time.</li> <li>Individual report view -- Full HTML report using the same   renderer as <code>safedump view --html</code>.</li> <li>Raw JSON endpoint -- Access report data as JSON via   <code>/api/reports/&lt;filename&gt;/raw</code>.</li> <li>Delete -- Delete reports via the API.</li> </ul>"},{"location":"web-server/#design-constraints","title":"Design Constraints","text":"<p>The server is intentionally minimal:</p> <ul> <li>No authentication -- Localhost only by default.</li> <li>No sessions -- Every request is stateless.</li> <li>No JavaScript framework -- Vanilla JS, no build step.</li> <li>No database -- Reads directly from the filesystem.</li> <li>stdlib only -- Built on <code>http.server</code>.</li> </ul>"},{"location":"web-server/#architecture","title":"Architecture","text":"<p>The server reuses the same <code>render_html()</code> function as the CLI HTML export. The report list is served from a vanilla JS single-page app embedded in the server module.</p>"},{"location":"frameworks/","title":"Framework Integration Guides","text":"<ul> <li>Flask: Error handler, WSGI middleware, request context</li> <li>FastAPI: Exception handler, ASGI middleware, lifespan, background tasks</li> <li>Django: Middleware class, logging integration, AppConfig, management command</li> </ul>"},{"location":"frameworks/django/","title":"Safedump + Django","text":""},{"location":"frameworks/django/#installation","title":"Installation","text":"<pre><code>pip install safedump[view]\n</code></pre> <p>Compatible with Django 3.2, 4.x, and 5.x.</p>"},{"location":"frameworks/django/#basic-setup","title":"Basic setup","text":""},{"location":"frameworks/django/#step-1-middleware-class","title":"Step 1: Middleware class","text":"<p>Create <code>middleware.py</code> in your Django project:</p> <pre><code># your_project/middleware.py\nimport safedump\n\n\nclass SafedumpMiddleware:\n    \"\"\"Captures unhandled exceptions and attaches crash IDs to requests.\"\"\"\n\n    def __init__(self, get_response):\n        self.get_response = get_response\n\n    def __call__(self, request):\n        return self.get_response(request)\n\n    def process_exception(self, request, exception):\n        report_path = safedump.capture_exception(exception)\n        request.safedump_crash_id = report_path.stem if report_path else None\n        return None  # Let Django's default error handling continue\n</code></pre>"},{"location":"frameworks/django/#step-2-register-middleware","title":"Step 2: Register middleware","text":"<p>In <code>settings.py</code>, place it first so it wraps the entire request lifecycle:</p> <pre><code>MIDDLEWARE = [\n    \"your_project.middleware.SafedumpMiddleware\",\n    # ...\n]\n</code></pre>"},{"location":"frameworks/django/#step-3-configure-safedump","title":"Step 3: Configure Safedump","text":"<p>In <code>settings.py</code> or your app config:</p> <pre><code>import safedump\nimport os\n\nsafedump.configure(\n    preset=\"production\",\n    output_dir=os.path.join(os.path.dirname(__file__), \"..\", \"crashes\"),\n)\nsafedump.install()\n</code></pre>"},{"location":"frameworks/django/#step-4-custom-500-handler-optional","title":"Step 4: Custom 500 handler (optional)","text":"<p>In <code>urls.py</code>, override the default error view to return a crash ID:</p> <pre><code>from django.http import JsonResponse\n\n\ndef handler500(request):\n    crash_id = getattr(request, \"safedump_crash_id\", None)\n    return JsonResponse({\"error\": \"Internal server error\", \"crash_id\": crash_id}, status=500)\n</code></pre>"},{"location":"frameworks/django/#recommended-configuration-for-production","title":"Recommended configuration for production","text":"<pre><code>import safedump\nimport os\n\nsafedump.configure(\n    preset=\"production\",\n    output_dir=os.environ.get(\"SAFEDUMP_DIR\", \"/var/log/safedump\"),\n    max_depth=5,\n)\nsafedump.install()\n</code></pre> <ul> <li>Use <code>preset=\"production\"</code>: privacy tier 1, environment variable values excluded.</li> <li>Absolute <code>output_dir</code>: use a path outside the project root. Mount a volume in Docker.</li> <li>Rotate reports: <code>safedump clean --older-than 30</code> via cron or management command.</li> <li>Environment variable for <code>output_dir</code>: different paths per environment.</li> </ul>"},{"location":"frameworks/django/#logging-integration","title":"Logging integration","text":"<p>Wire Safedump into Django's logging framework:</p> <pre><code>import logging\n\nlogger = logging.getLogger(\"safedump\")\n\n\nclass SafedumpLoggingMiddleware:\n    def __init__(self, get_response):\n        self.get_response = get_response\n\n    def __call__(self, request):\n        return self.get_response(request)\n\n    def process_exception(self, request, exception):\n        report_path = safedump.capture_exception(exception)\n        logger.error(\n            \"Unhandled exception captured. Crash report: %s\",\n            report_path,\n            exc_info=True,\n            extra={\"crash_report\": str(report_path), \"path\": request.path},\n        )\n        return None\n</code></pre>"},{"location":"frameworks/django/#appconfig-startup","title":"AppConfig startup","text":"<p>Initialize Safedump once at startup via <code>AppConfig.ready()</code>:</p> <pre><code># your_project/apps.py\nfrom django.apps import AppConfig\n\n\nclass YourProjectConfig(AppConfig):\n    name = \"your_project\"\n\n    def ready(self):\n        import safedump\n        import os\n\n        safedump.configure(\n            preset=\"production\",\n            output_dir=os.environ.get(\"SAFEDUMP_DIR\", \"/var/log/safedump\"),\n        )\n        safedump.install()\n</code></pre>"},{"location":"frameworks/django/#complete-working-example","title":"Complete working example","text":"<pre><code>your_project/\n\u251c\u2500\u2500 middleware.py       # SafedumpMiddleware\n\u251c\u2500\u2500 apps.py             # AppConfig with safedump.init\n\u251c\u2500\u2500 settings.py         # MIDDLEWARE registration\n\u251c\u2500\u2500 urls.py             # handler500, debug crash endpoint\n\u2514\u2500\u2500 management/\n    \u2514\u2500\u2500 commands/\n        \u2514\u2500\u2500 cleancrashes.py  # Report rotation command\n</code></pre> <p><code>middleware.py</code>:</p> <pre><code>import safedump\n\n\nclass SafedumpMiddleware:\n    def __init__(self, get_response):\n        self.get_response = get_response\n\n    def __call__(self, request):\n        return self.get_response(request)\n\n    def process_exception(self, request, exception):\n        report_path = safedump.capture_exception(exception)\n        request.safedump_crash_id = report_path.stem if report_path else None\n        return None\n</code></pre> <p><code>management/commands/cleancrashes.py</code>:</p> <pre><code>from django.core.management.base import BaseCommand\nimport subprocess\n\n\nclass Command(BaseCommand):\n    help = \"Delete Safedump crash reports older than N days.\"\n\n    def add_arguments(self, parser):\n        parser.add_argument(\"--older-than\", type=int, default=30)\n\n    def handle(self, *args, **options):\n        days = options[\"older_than\"]\n        subprocess.run([\"safedump\", \"clean\", \"--older-than\", str(days)], check=True)\n        self.stdout.write(self.style.SUCCESS(f\"Cleaned reports older than {days} days.\"))\n</code></pre>"},{"location":"frameworks/django/#testing-instructions","title":"Testing instructions","text":"<p>Add a crash endpoint:</p> <pre><code># urls.py\nfrom django.urls import path\nfrom django.http import HttpResponse\n\n\ndef trigger_crash(request):\n    x = None\n    return HttpResponse(x.upper())  # AttributeError\n\n\nurlpatterns = [\n    path(\"__debug__/crash/\", trigger_crash),\n]\n</code></pre> <pre><code>python manage.py runserver\ncurl http://localhost:8000/__debug__/crash/\nsafedump view\n</code></pre>"},{"location":"frameworks/fastapi/","title":"Safedump + FastAPI","text":""},{"location":"frameworks/fastapi/#installation","title":"Installation","text":"<pre><code>pip install safedump[view] fastapi uvicorn\n</code></pre>"},{"location":"frameworks/fastapi/#basic-setup","title":"Basic setup","text":"<p>Register a FastAPI exception handler that captures crash context before returning an error response:</p> <pre><code>import safedump\nfrom fastapi import FastAPI, Request\nfrom fastapi.responses import JSONResponse\n\nsafedump.configure(preset=\"production\", output_dir=\"./crashes\")\n\napp = FastAPI()\n\n\n@app.exception_handler(Exception)\nasync def safedump_exception_handler(request: Request, exc: Exception):\n    report_path = safedump.capture_exception(exc)\n    return JSONResponse(\n        status_code=500,\n        content={\n            \"error\": \"Internal server error\",\n            \"crash_id\": report_path.stem if report_path else None,\n        },\n    )\n</code></pre> <p>For broader coverage (startup failures, middleware errors), add ASGI middleware:</p> <pre><code>from starlette.middleware.base import BaseHTTPMiddleware\n\n\nclass SafedumpMiddleware(BaseHTTPMiddleware):\n    async def dispatch(self, request, call_next):\n        try:\n            return await call_next(request)\n        except Exception:\n            safedump.capture_exception()\n            raise\n\n\napp.add_middleware(SafedumpMiddleware)\n</code></pre>"},{"location":"frameworks/fastapi/#lifespan-integration","title":"Lifespan integration","text":"<p>Install Safedump at startup and clean up at shutdown:</p> <pre><code>from contextlib import asynccontextmanager\n\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    safedump.configure(preset=\"production\", output_dir=\"./crashes\")\n    safedump.install()\n    yield\n    safedump.uninstall()\n\n\napp = FastAPI(lifespan=lifespan)\n</code></pre>"},{"location":"frameworks/fastapi/#background-task-protection","title":"Background task protection","text":"<p>FastAPI background tasks run after the response is sent: exceptions inside them are not caught by exception handlers:</p> <pre><code>from fastapi import BackgroundTasks\n\n\ndef risky_job():\n    try:\n        dangerous_operation()\n    except Exception:\n        safedump.capture_exception()\n\n\n@app.post(\"/process\")\nasync def process(background_tasks: BackgroundTasks):\n    background_tasks.add_task(risky_job)\n    return {\"status\": \"accepted\"}\n</code></pre>"},{"location":"frameworks/fastapi/#recommended-configuration-for-production","title":"Recommended configuration for production","text":"<pre><code>safedump.configure(\n    preset=\"production\",\n    output_dir=\"/var/log/safedump\",\n    max_depth=5,\n)\nsafedump.install()\n</code></pre> <ul> <li><code>preset=\"production\"</code>: privacy tier 1, environment variable values excluded.</li> <li>Persist <code>output_dir</code>: mount a volume at <code>/var/log/safedump</code> in Docker/Kubernetes.</li> <li>Rotate periodically: <code>safedump clean --older-than 30</code> via cron.</li> <li>Crash ID in responses: include report filename in 500 responses so users can reference it.</li> </ul>"},{"location":"frameworks/fastapi/#complete-working-example","title":"Complete working example","text":"<pre><code># app.py\nimport safedump\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI, Request\nfrom fastapi.responses import JSONResponse\n\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    safedump.configure(preset=\"production\", output_dir=\"./crashes\")\n    safedump.install()\n    yield\n    safedump.uninstall()\n\n\napp = FastAPI(lifespan=lifespan)\n\n\n@app.exception_handler(Exception)\nasync def safedump_exception_handler(request: Request, exc: Exception):\n    report_path = safedump.capture_exception(exc)\n    return JSONResponse(\n        status_code=500,\n        content={\n            \"error\": \"Internal server error\",\n            \"crash_id\": report_path.stem if report_path else None,\n        },\n    )\n\n\n@app.get(\"/api/data\")\nasync def get_data():\n    return {\"data\": [1, 2, 3]}\n\n\nif __name__ == \"__main__\":\n    import uvicorn\n\n    uvicorn.run(app)\n</code></pre>"},{"location":"frameworks/fastapi/#testing-instructions","title":"Testing instructions","text":"<pre><code>@app.get(\"/__debug__/crash\")\nasync def trigger_crash():\n    x = None\n    return x.upper()\n</code></pre> <pre><code>uvicorn app:app --reload\ncurl http://localhost:8000/__debug__/crash\nsafedump view\n</code></pre>"},{"location":"frameworks/flask/","title":"Safedump + Flask","text":""},{"location":"frameworks/flask/#installation","title":"Installation","text":"<pre><code>pip install safedump[view]\n</code></pre> <p>No Flask-specific dependencies required. Works with any Flask version.</p>"},{"location":"frameworks/flask/#basic-setup","title":"Basic setup","text":"<p>Register a custom error handler that captures crash context before returning the error response:</p> <pre><code>import safedump\nfrom flask import Flask, jsonify\nfrom werkzeug.exceptions import HTTPException\n\nsafedump.configure(preset=\"production\", output_dir=\"./crashes\")\n\napp = Flask(__name__)\n\n\n@app.errorhandler(Exception)\ndef handle_unhandled(exc):\n    \"\"\"Capture crash context for all unhandled exceptions.\"\"\"\n    if isinstance(exc, HTTPException):\n        return jsonify(error=exc.description), exc.code\n\n    report_path = safedump.capture_exception(exc)\n    app.logger.error(\"Crash report saved: %s\", report_path)\n\n    return jsonify(error=\"Internal server error\", crash_id=report_path.stem), 500\n</code></pre> <p>For broader coverage (exceptions outside request handlers), add WSGI middleware:</p> <pre><code>class SafedumpMiddleware:\n    \"\"\"WSGI middleware: captures exceptions that escape the app.\"\"\"\n\n    def __init__(self, app):\n        self.app = app\n\n    def __call__(self, environ, start_response):\n        try:\n            return self.app(environ, start_response)\n        except Exception:\n            safedump.capture_exception()\n            raise\n\n\napp.wsgi_app = SafedumpMiddleware(app.wsgi_app)\n</code></pre> <p>Combine both: middleware catches framework-level errors, error handler produces user-friendly responses.</p>"},{"location":"frameworks/flask/#recommended-configuration-for-production","title":"Recommended configuration for production","text":"<pre><code>import safedump\n\nsafedump.configure(\n    preset=\"production\",  # privacy tier 1, no env capture, no argv\n    output_dir=\"/var/log/safedump\",\n    max_depth=5,\n)\nsafedump.install()\n</code></pre> <ul> <li><code>preset=\"production\"</code>: disables environment variable values and CLI argument capture.</li> <li>Persist <code>output_dir</code>: use a mounted volume so reports survive container restarts.</li> <li>Rotate reports: <code>safedump clean --older-than 30</code> via cron.</li> <li>Never use privacy tier 4 in production: it captures raw environment variable values.</li> </ul>"},{"location":"frameworks/flask/#attach-request-context","title":"Attach request context","text":"<p>Enrich crash reports with request metadata:</p> <pre><code>from flask import request, g\n\n\ndef add_request_context(report):\n    try:\n        report.metadata[\"request\"] = {\n            \"method\": request.method,\n            \"path\": request.path,\n            \"remote_addr\": request.remote_addr,\n        }\n        if hasattr(g, \"user_id\"):\n            report.metadata[\"user_id\"] = g.user_id\n    except RuntimeError:\n        pass  # outside request context\n    return report\n\n\nsafedump.configure(preset=\"production\", before_capture=add_request_context)\n</code></pre>"},{"location":"frameworks/flask/#complete-working-example","title":"Complete working example","text":"<pre><code># app.py\nimport safedump\nfrom flask import Flask, jsonify, request\nfrom werkzeug.exceptions import HTTPException\n\n\ndef _add_request(report):\n    try:\n        report.metadata[\"request\"] = {\"method\": request.method, \"path\": request.path}\n    except RuntimeError:\n        pass\n    return report\n\n\nsafedump.configure(\n    preset=\"production\",\n    output_dir=\"./crashes\",\n    before_capture=_add_request,\n)\nsafedump.install()\n\napp = Flask(__name__)\n\n\n@app.errorhandler(Exception)\ndef handle_unhandled(exc):\n    if isinstance(exc, HTTPException):\n        return jsonify(error=exc.description), exc.code\n\n    path = safedump.capture_exception(exc)\n    app.logger.error(\"Crash report: %s\", path)\n    return jsonify(error=\"Internal server error\"), 500\n\n\n@app.route(\"/api/data\")\ndef get_data():\n    return jsonify([1, 2, 3])\n\n\nif __name__ == \"__main__\":\n    app.run()\n</code></pre>"},{"location":"frameworks/flask/#testing-instructions","title":"Testing instructions","text":"<p>Add a debug crash endpoint:</p> <pre><code>@app.route(\"/__debug__/crash\")\ndef trigger_crash():\n    x = None\n    return x.upper()  # AttributeError\n</code></pre> <pre><code>flask run\ncurl http://localhost:5000/__debug__/crash\nsafedump view\n</code></pre> <p>Remove the debug endpoint before deploying to production.</p>"},{"location":"getting-started/configuration/","title":"Configuration","text":"<p>Call <code>safedump.configure()</code> before <code>safedump.install()</code>. All parameters are keyword-only.</p> <pre><code>import safedump\n\nsafedump.configure(\n    preset=\"production\",\n    output_dir=\"./crashes\",\n    privacy_tier=1,\n)\nsafedump.install()\n</code></pre>"},{"location":"getting-started/configuration/#parameters","title":"Parameters","text":"Parameter Default Description <code>preset</code> <code>None</code> Quick configuration preset <code>output_dir</code> <code>~/.safedump</code> Directory for crash reports <code>privacy_tier</code> <code>1</code> Capture detail level (0-4) <code>include_env_names</code> <code>True</code> Include env var names <code>include_argv</code> <code>False</code> Include command-line args <code>max_string_length</code> <code>10000</code> Max chars per captured string <code>max_collection_items</code> <code>100</code> Max items from collections <code>max_depth</code> <code>5</code> Max depth for serialization <code>redaction_rules</code> <code>[]</code> Custom redaction patterns <code>before_capture</code> <code>None</code> Pre-processing callback <code>enable_entropy_detection</code> <code>False</code> Entropy-based secret detection <code>entropy_threshold</code> <code>4.5</code> Entropy threshold (bits/char) <code>compress</code> <code>False</code> Gzip compress crash reports <code>on_crash</code> <code>None</code> Post-capture callback"},{"location":"getting-started/configuration/#presets","title":"Presets","text":"Preset Tier Env Vars Argv Depth <code>production</code> 1 Yes No 5 <code>development</code> 2 Yes Yes 10 <code>debug</code> 4 Yes Yes 20 <code>minimal</code> 0 No No 3"},{"location":"getting-started/configuration/#examples","title":"Examples","text":""},{"location":"getting-started/configuration/#production-configuration","title":"Production configuration","text":"<pre><code>safedump.configure(\n    preset=\"production\",\n    compress=True,\n    enable_entropy_detection=True,\n)\n</code></pre>"},{"location":"getting-started/configuration/#custom-output-directory-with-notification","title":"Custom output directory with notification","text":"<pre><code>def notify(path):\n    import subprocess\n\n    subprocess.Popen([\"notify-send\", f\"Crash saved: {path}\"])\n\n\nsafedump.configure(\n    output_dir=\"/var/log/crashes\",\n    on_crash=notify,\n)\n</code></pre>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>Safedump requires Python 3.9 or later. It has zero required dependencies -- the core capture and CLI work out of the box.</p>"},{"location":"getting-started/installation/#standard-install","title":"Standard Install","text":"<pre><code>pip install safedump\n</code></pre> <p>This installs the core library with basic CLI support (plain text output).</p>"},{"location":"getting-started/installation/#with-terminal-viewer","title":"With Terminal Viewer","text":"<p>For Rich-powered terminal output with syntax highlighting:</p> <pre><code>pip install safedump[view]\n</code></pre> <p>This adds the <code>rich</code> and <code>pygments</code> dependencies.</p>"},{"location":"getting-started/installation/#minimal-install","title":"Minimal Install","text":"<p>If you only need crash capture (no CLI):</p> <pre><code>pip install safedump --no-deps\n</code></pre>"},{"location":"getting-started/installation/#verify-installation","title":"Verify Installation","text":"<pre><code>safedump --version\n</code></pre> <p>Should print <code>safedump 1.3.0</code> (or later).</p>"},{"location":"getting-started/installation/#platform-support","title":"Platform Support","text":"Platform Status Linux Primary development platform macOS Fully supported Windows Verified on Windows 11"},{"location":"getting-started/quickstart/","title":"Quick Start","text":""},{"location":"getting-started/quickstart/#one-line-setup","title":"One-line Setup","text":"<pre><code>import safedump\n\nsafedump.install()\n</code></pre> <p>That's it. Every unhandled exception now produces a structured crash report in <code>~/.safedump/</code>.</p>"},{"location":"getting-started/quickstart/#view-a-crash-report","title":"View a Crash Report","text":"<pre><code>safedump view\n</code></pre> <p>This shows the most recent crash with syntax highlighting (if Rich is installed) or plain text.</p>"},{"location":"getting-started/quickstart/#export-as-html","title":"Export as HTML","text":"<pre><code>safedump view --html report.html\n# open report.html in any browser\n</code></pre> <p>The HTML file is completely self-contained -- no internet required.</p>"},{"location":"getting-started/quickstart/#manual-capture","title":"Manual Capture","text":"<pre><code>try:\n    result = dangerous_operation()\nexcept Exception:\n    path = safedump.capture_exception()\n    print(f\"Crash captured: {path}\")\n    raise  # re-raise after capture\n</code></pre>"},{"location":"getting-started/quickstart/#list-recent-crashes","title":"List Recent Crashes","text":"<pre><code>safedump list\n</code></pre>"},{"location":"getting-started/quickstart/#filter-by-exception-type","title":"Filter by Exception Type","text":"<pre><code>safedump list --type KeyError --since 7d\n</code></pre>"},{"location":"getting-started/quickstart/#run-a-self-test","title":"Run a Self-Test","text":"<pre><code>safedump test\n</code></pre> <p>This deliberately raises and captures a test exception to verify Safedump is working.</p>"}]}